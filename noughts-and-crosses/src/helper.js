export function calculateWinner(boardArray) {
    // INPUT = the current state of the board
    // OUTPUT = X if Player X has won,
    // OUTPUT = Y if Player Y has won,
    // OUTPUT = null if nonone has yet won

    //TODO: obviously add in all the calculate winner logic!
    return null; //to mock a game in progress
    // return "X"; //to mock X winning
    // return "O"; // to mock O winning
}
